SET 'auto.offset.reset'='earliest';

/* Q1 */
SELECT KM.KEY AS KEY,
		KM.NV AS V,
		SUM(NC.N) AS N
FROM ADW06_KEYMON KM
	INNER JOIN ADW06_NACC NC
	ON KM.KEY = NC.KEY
GROUP BY KM.KEY, KM.NV
EMIT CHANGES;
SET 'auto.offset.reset'='earliest';

/* COUNT ACCESES */
CREATE TABLE IF NOT EXISTS adw06_nacc
	WITH (KAFKA_TOPIC='adw.adw06.nacc', VALUE_FORMAT='AVRO', KEY_FORMAT='AVRO')
	AS SELECT
		STRUCT(IP:=IP, UAID:=UAID) as KEY,
		COUNT(*) as N
	FROM ADW06_KEYACC
	GROUP BY STRUCT(IP:=IP, UAID:=UAID);